#pragma once

// USB Drive (export) mode screen - shown when the device booted with 'e' held
// and the SD card is being exposed to a host PC over USB Mass Storage.
void UsbDriveScreen_setup();
void UsbDriveScreen_render();
